# --- App server port (you already run on 9090)
server.port=9090

# --- Spring Mail (UTD SMTP) ---
spring.mail.host=smtp.utdallas.edu
spring.mail.port=587
spring.mail.username=ank210005@utdallas.edu
spring.mail.password=${UTD_SMTP_PASSWORD}    # put real password in an env var, not in Git
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=10000
spring.mail.properties.mail.smtp.timeout=10000
spring.mail.properties.mail.smtp.writetimeout=10000

# --- Who receives new requests (comma-separated) ---
club.admin.emails=ank210005@utdallas.edu,kxg220063@utdallas.edu

# --- From address (the sender shown in the email) ---
club.from.email=ank210005@utdallas.edu

# Disable Thymeleaf template cache for faster development (auto-refresh on save)
spring.thymeleaf.cache=false

sendgrid.api-key=${SENDGRID_API_KEY}


